---
description: Khởi chạy luồng Agent Xây Cổng thông tin (Convert Txt/Word sang Website)
---
This workflow runs the Static Site Generator (SSG). It sweeps the unstructured text files scattered in the `Raw_Documents` directory and elegantly injects them into the modern Glassmorphism HTML Template, resulting in a compiled ready-to-deploy `index.html`.

// turbo-all
1. Kích hoạt luồng Agent Nhào nặn Website Nội Bộ
```bash
cd "e:\Antigravity\e-learning\Internal Portal (Vercel Deploy)"
python generate_portal.py
```
